# deps
node_modules/

# build (committed to main by the deploy workflow via `git add -f dist`)
dist/
*.local

# env / secrets
.env
.env.*
!.env.example
*.key

# opencode local config (contains MCP API keys) - keep out of repo
opencode.json

# os / editor
.DS_Store
*.log
.idea/
.vscode/
*.tsbuildinfo
